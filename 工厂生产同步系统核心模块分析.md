# 工厂生产同步系统 - 核心模块分析

## 1. 项目概述

**工厂生产同步系统**（Factory Production Synchronization System）是一个基于 Java 内置并发机制实现的经典生产者-消费者（Producer-Consumer）模式演示项目。该项目模拟工厂中经理（Manager）分配生产任务、组长（Team Leader）领取任务并组织生产的同步协作过程。

**项目背景**：
- 经理每日将生产类型（pID）放入任务盒
- 组长从任务盒中取出生产类型，组织团队制造
- 任务盒配备布尔锁机制，确保"放入"操作始终先于"取出"操作
- 通过 `wait()`/`notify()` 实现线程间的条件同步

---

## 2. 技术栈

| 层级 | 技术 | 版本 | 说明 |
|------|------|------|------|
| **编程语言** | Java | 17 | LTS 版本，原生支持 `synchronized`/`wait`/`notify` |
| **构建工具** | Maven | 3.6+ | 标准 Java 项目构建和依赖管理 |
| **日志框架** | SLF4J | 2.0.9 | 日志门面，提供统一的日志 API |
| **日志实现** | Logback | 1.4.14 | SLF4J 的原生实现，高性能异步日志 |
| **并发模型** | Java 内置锁 | - | `synchronized` + `wait()` + `notify()` |
| **容器化** | Docker | - | 多阶段构建，镜像轻量化 |
| **编排工具** | Docker Compose | - | 一键容器化部署 |

### 2.1 技术选型理由

1. **Java 17**：题目明确要求使用 `wait()`/`notify()` 机制，Java 是实现该并发模型的原生语言
2. **Maven**：Java 生态最成熟的构建工具，支持依赖管理和插件生态
3. **SLF4J + Logback**：Java 日志的事实标准，支持结构化日志输出和灵活的配置
4. **Docker 多阶段构建**：
   - 第一阶段使用 Maven 镜像编译构建
   - 第二阶段仅使用 JRE 运行，减小镜像体积
   - 构建缓存优化，提升 CI/CD 效率

---

## 3. 系统架构

### 3.1 整体架构图

```
┌──────────────────────────────────────────────────────────────────────┐
│                         Main 入口线程                                 │
│  [Main.java]                                                         │
│  ├─ 创建 Factory 共享实例                                            │
│  ├─ 创建 Manager 生产者线程                                          │
│  ├─ 创建 TeamLeader 消费者线程                                       │
│  ├─ 注册 ShutdownHook 优雅关闭钩子                                   │
│  └─ 启动并等待两个线程结束                                           │
└─────────────────────────────────────┬────────────────────────────────┘
                                      │
                                      ▼
                        ┌──────────────────────────┐
                        │  Factory 共享任务盒       │
                        │  [model/Factory.java]    │
                        │  ├─ pID: int             │
                        │  ├─ lock: boolean        │
                        │  ├─ put(pID)             │
                        │  └─ get()                │
                        └───────────┬──────────────┘
                                    │
                ┌───────────────────┴───────────────────┐
                │                                       │
                ▼                                       ▼
    ┌───────────────────────────┐         ┌──────────────────────────────┐
    │ ManagerProcess 生产者     │         │ TeamLeaderProcess 消费者     │
    │ [process/ManagerProcess.java]│      │ [process/TeamLeaderProcess.java]│
    │  ├─ 循环生成 pID 1..n     │         │  ├─ 循环调用 get() 取任务    │
    │  ├─ 调用 factory.put(pID) │         │  ├─ 组织团队制造（模拟）     │
    │  └─ Sleep 1s 模拟每日间隔 │         │  └─ Sleep 1.5s 模拟生产时间  │
    └───────────────────────────┘         └──────────────────────────────┘
```

### 3.2 架构分层

| 层级 | 职责 | 核心类 |
|------|------|--------|
| **入口层** | 程序启动、线程管理、优雅关闭 | [Main.java](file:///Users/zhangchengcheng/work/ai-project/Trea/solo0605/label-02361/backend/src/main/java/com/factory/Main.java) |
| **模型层** | 共享资源、同步逻辑封装 | [Factory.java](file:///Users/zhangchengcheng/work/ai-project/Trea/solo0605/label-02361/backend/src/main/java/com/factory/model/Factory.java) |
| **进程层** | 业务逻辑、生产者/消费者行为 | [ManagerProcess.java](file:///Users/zhangchengcheng/work/ai-project/Trea/solo0605/label-02361/backend/src/main/java/com/factory/process/ManagerProcess.java)、[TeamLeaderProcess.java](file:///Users/zhangchengcheng/work/ai-project/Trea/solo0605/label-02361/backend/src/main/java/com/factory/process/TeamLeaderProcess.java) |

---

## 4. 核心模块详解

### 4.1 Factory 类 - 核心同步模型

**文件**：[Factory.java](file:///Users/zhangchengcheng/work/ai-project/Trea/solo0605/label-02361/backend/src/main/java/com/factory/model/Factory.java#L25-L122)

**核心字段**：
| 字段 | 类型 | 初始值 | 说明 |
|------|------|--------|------|
| `pID` | `int` | 0 | 存储生产类型 ID（1..n） |
| `lock` | `boolean` | `false` | 任务盒锁状态：`false`=打开/空，`true`=上锁/有任务 |

#### 4.1.1 `put(int pID)` 方法 - 生产者放入

```java
public synchronized void put(int pID) {
    // 条件等待：锁已上锁（盒子有任务）时等待
    while (lock) {
        logger.debug("经理等待中 - 任务盒已上锁（pID={} 尚未被处理）", this.pID);
        wait();
    }
    
    // 放入生产类型
    this.pID = pID;
    
    // 上锁并通知消费者
    lock = true;
    logger.info("【经理】放入生产类型 pID={} → lock=true，调用 notify()", pID);
    notify();
}
```

**执行流程**：
1. 获取 Factory 对象的内置锁（`synchronized` 方法）
2. 检查 `lock` 状态，若为 `true` 则调用 `wait()` 释放锁并进入等待队列
3. 被唤醒后重新检查条件（`while` 循环防止虚假唤醒）
4. 设置 `pID`，将 `lock` 置为 `true`（上锁）
5. 调用 `notify()` 唤醒等待队列中的一个线程（组长）

#### 4.1.2 `get()` 方法 - 消费者取出

```java
public synchronized int get() {
    // 条件等待：锁未上锁（盒子为空）时等待
    while (!lock) {
        logger.debug("组长等待中 - 任务盒为空，调用 wait() 阻塞");
        wait();
    }
    
    // 用钥匙开锁
    lock = false;
    int result = this.pID;
    
    // 通知生产者并返回
    logger.info("【组长】取出 pID={} → lock=false，调用 notify()", result);
    notify();
    return result;
}
```

**执行流程**：
1. 获取 Factory 对象的内置锁
2. 检查 `lock` 状态，若为 `false` 则调用 `wait()` 释放锁并等待
3. 被唤醒后重新检查条件
4. 将 `lock` 置为 `false`（开锁），读取 `pID`
5. 调用 `notify()` 唤醒等待队列中的线程（经理）
6. 返回生产类型 ID

### 4.2 ManagerProcess 类 - 生产者进程

**文件**：[ManagerProcess.java](file:///Users/zhangchengcheng/work/ai-project/Trea/solo0605/label-02361/backend/src/main/java/com/factory/process/ManagerProcess.java#L27-L91)

**职责**：模拟经理每日分配生产任务的过程

**核心逻辑**：
```java
@Override
public void run() {
    int pID = 1;
    int dayCount = 0;
    
    while (!Thread.currentThread().isInterrupted()) {
        dayCount++;
        logger.info("[第 {} 天] 经理准备分配 pID={}", dayCount, pID);
        
        // 放入任务盒（可能阻塞）
        factory.put(pID);
        
        // 循环 1→2→...→n→1
        pID = (pID % maxProductionTypes) + 1;
        
        // 模拟每日间隔（1秒=1天）
        Thread.sleep(1000);
    }
}
```

**关键特性**：
- 生产类型循环：`pID = (pID % n) + 1` 实现 1..n 循环
- 可中断循环：通过 `isInterrupted()` 检查支持优雅关闭
- 参数校验：构造函数中校验 `factory` 非空、`maxProductionTypes >= 1`
- 异常处理：`InterruptedException` 时恢复中断状态

### 4.3 TeamLeaderProcess 类 - 消费者进程

**文件**：[TeamLeaderProcess.java](file:///Users/zhangchengcheng/work/ai-project/Trea/solo0605/label-02361/backend/src/main/java/com/factory/process/TeamLeaderProcess.java#L29-L87)

**职责**：模拟组长领取任务、组织生产的过程

**核心逻辑**：
```java
@Override
public void run() {
    int taskCount = 0;
    
    while (!Thread.currentThread().isInterrupted()) {
        // 从任务盒取任务（可能阻塞）
        int pID = factory.get();
        if (pID == -1) break;  // 等待时被中断
        
        taskCount++;
        logger.info("[任务 #{}] 开始制造 pID={}", taskCount, pID);
        
        // 模拟制造时间（1.5秒）
        Thread.sleep(1500);
        logger.info("[任务 #{}] pID={} 制造完成！", taskCount, pID);
    }
}
```

**关键特性**：
- 阻塞获取：`get()` 方法在任务盒为空时自动阻塞
- 中断处理：`get()` 返回 `-1` 表示等待时被中断
- 生产模拟：1.5 秒的 sleep 模拟制造过程（比经理分配慢，形成生产瓶颈）

### 4.4 Main 类 - 程序入口

**文件**：[Main.java](file:///Users/zhangchengcheng/work/ai-project/Trea/solo0605/label-02361/backend/src/main/java/com/factory/Main.java#L28-L116)

**核心职责**：
1. **参数解析**：从命令行读取生产类型数量，默认 5
2. **对象创建**：创建共享的 Factory 实例
3. **线程管理**：创建并命名 Manager 和 TeamLeader 线程
4. **优雅关闭**：注册 JVM 关闭钩子，确保线程安全退出
5. **生命周期管理**：启动线程并等待结束

#### 4.4.1 优雅关闭机制

```java
Runtime.getRuntime().addShutdownHook(new Thread(() -> {
    logger.info("收到关闭信号 - 正在停止所有进程...");
    
    // 发送中断信号
    managerThread.interrupt();
    teamLeaderThread.interrupt();
    
    // 等待线程退出（最多3秒）
    managerThread.join(3000);
    teamLeaderThread.join(3000);
    
    logger.info("所有进程已停止，系统关闭完成。");
}, "关闭钩子"));
```

**设计要点**：
- 调用 `interrupt()` 而不是 `stop()`（`stop()` 已废弃）
- 等待线程有机会清理资源（3秒超时防止死锁）
- 关闭钩子线程有独立名称便于调试

---

## 5. 同步机制详解

### 5.1 状态转换图

```
┌─────────────────┐      put      ┌─────────────────┐
│   Box Empty     │──────────────▶│    Box Full     │
│  lock = false   │   lock=true   │  lock = true    │
│  pID 可写入     │   notify()    │  pID 可读取     │
└─────────────────┘               └─────────────────┘
          ▲                                │
          │                                │
          │ get()                          │ put()
          │ lock=false                     │ wait()
          │ notify()                       │
          │                                ▼
┌─────────────────┐               ┌─────────────────┐
│ TeamLeaderWait  │               │  ManagerWait    │
│  wait() 阻塞     │               │  wait() 阻塞     │
└─────────────────┘               └─────────────────┘
          │                                │
          │ notify()                       │ notify()
          └────────────────────────────────┘
```

### 5.2 同步规则矩阵

| 操作 | 当前状态 lock=false | 当前状态 lock=true |
|------|-------------------|-------------------|
| **Manager put()** | ✅ 执行放入 → lock=true → notify() | ⏳ wait() 等待 |
| **TeamLeader get()** | ⏳ wait() 等待 | ✅ 开锁 → lock=false → get → notify() |

### 5.3 关键技术点

#### 5.3.1 `synchronized` 关键字
- **作用**：确保同一时间只有一个线程可以执行 `put()` 或 `get()` 方法
- **锁对象**：Factory 实例本身（方法级 synchronized）
- **内存语义**：确保锁释放前的写入对后续获取锁的线程可见

#### 5.3.2 `wait()` 方法
- **作用**：释放当前持有的锁，进入等待队列
- **必须在 synchronized 块内调用**：否则抛出 `IllegalMonitorStateException`
- **使用 while 循环检查条件**：防止虚假唤醒（spurious wakeup）

#### 5.3.3 `notify()` 方法
- **作用**：唤醒等待队列中的一个线程
- **不会立即释放锁**：调用后仍需退出 synchronized 块才会释放锁
- **与 notifyAll() 的选择**：本题只有两个线程，`notify()` 足够且效率更高

---

## 6. 时序交互示例

以下展示前两轮生产的完整交互时序：

```
时间轴 →
  │
  ├─ Manager 第1天: put(1) → lock=true → notify()
  │     └─ 日志: 【经理】放入 pID=1 → lock=true
  │
  ├─ TeamLeader 被唤醒: get() → lock=false → notify()
  │     └─ 日志: 【组长】取出 pID=1 → lock=false
  │     └─ 开始制造 pID=1（sleep 1.5s）
  │
  ├─ Manager 第2天: 检查 lock=false → put(2) → lock=true → notify()
  │     └─ 日志: 【经理】放入 pID=2 → lock=true
  │
  ├─ TeamLeader 制造完成（1.5s后）: 日志 "[任务 #1] 制造完成"
  │
  ├─ TeamLeader 再次调用 get(): lock=true → 取出 pID=2 → lock=false
        └─ 日志: 【组长】取出 pID=2 → 开始制造
```

---

## 7. 部署架构

### 7.1 Docker 多阶段构建

**Dockerfile** 采用两阶段构建策略：

```
阶段一（构建）            阶段二（运行）
┌─────────────────┐     ┌─────────────────┐
│ maven:3.9.9-    │     │ eclipse-temurin:│
│ eclipse-temurin-17 │     │ 17-jre          │
│                 │     │                 │
│ 1. 复制 pom.xml │     │ 1. 设置 UTF-8    │
│ 2. 下载依赖     │────▶│ 2. 复制 jar 包   │
│ 3. 编译打包     │     │ 3. 启动应用      │
└─────────────────┘     └─────────────────┘
```

**优势**：
- 最终镜像仅 ~300MB（JRE），不含 Maven 和编译工具
- 依赖层缓存，后续构建更快
- 运行环境干净，减少攻击面

### 7.2 Docker Compose 编排

**docker-compose.yml** 配置：
```yaml
services:
  factory-sync:
    build: ./backend
    container_name: factory-sync
    restart: unless-stopped
    command: ["${PRODUCTION_TYPES:-5}"]  # 支持环境变量覆盖
    environment:
      TZ: Asia/Shanghai                   # 时区设置
```

---

## 8. 代码质量与设计原则

### 8.1 设计原则遵循

| 原则 | 体现 |
|------|------|
| **单一职责** | Factory 只负责同步逻辑，Manager/TeamLeader 只负责业务流程 |
| **封装性** | `pID` 和 `lock` 为 private，仅通过 `put()`/`get()` 访问 |
| **依赖注入** | Factory 通过构造函数注入到两个 Process 中 |
| **线程安全** | 所有共享状态访问都通过 synchronized 保护 |
| **防御式编程** | 构造函数参数校验、中断处理、while 循环防虚假唤醒 |

### 8.2 异常处理

1. **InterruptedException 处理**：
   - 捕获后调用 `Thread.currentThread().interrupt()` 恢复中断状态
   - 便于外层循环检测中断标志

2. **参数校验**：
   - Factory 非空检查
   - 生产类型数量 >= 1 检查

3. **边界情况**：
   - 命令行参数解析失败时回退到默认值
   - 非法参数时记录警告日志

---

## 9. 项目结构

```
label-02361/
├── docker-compose.yml                     # Docker Compose 编排
├── start.sh                              # Linux/macOS 一键启动
├── start.bat                             # Windows 一键启动
├── README.md                             # 项目说明
├── docs/
│   └── project_design.md                 # 详细设计文档
└── backend/
    ├── Dockerfile                        # 多阶段构建
    ├── .dockerignore
    ├── pom.xml                           # Maven 配置
    └── src/main/
        ├── java/com/factory/
        │   ├── Main.java                 # 入口类
        │   ├── model/
        │   │   └── Factory.java          # 核心同步模型
        │   └── process/
        │       ├── ManagerProcess.java   # 生产者
        │       └── TeamLeaderProcess.java # 消费者
        └── resources/
            └── logback.xml               # 日志配置
```

---

## 10. 运行方式

### 10.1 方式一：Docker Compose（推荐）
```bash
# 启动
docker-compose up --build -d

# 查看日志
docker-compose logs -f

# 指定生产类型数量
PRODUCTION_TYPES=10 docker-compose up --build -d
```

### 10.2 方式二：启动脚本
```bash
./start.sh 10    # Linux/macOS，使用10种生产类型
start.bat 10     # Windows
```

### 10.3 方式三：手动构建
```bash
cd backend
mvn clean package -DskipTests
java -jar target/factory-sync-1.0.0.jar 10
```

---

## 11. 总结

**工厂生产同步系统**是一个经典的 Java 并发编程教学项目，其核心价值在于：

1. **教学意义**：清晰展示了 `wait()`/`notify()` 机制的正确用法
2. **设计规范**：遵循 Java 并发编程最佳实践（while 循环、中断处理、内存可见性）
3. **工程化**：完整的 Maven 构建、日志配置、容器化部署
4. **可维护性**：代码分层清晰，注释完善，易于理解和扩展

该项目虽然代码量不大（约 300 行核心代码），但涵盖了 Java 并发编程的核心知识点，是学习生产者-消费者模式和 `wait()`/`notify()` 机制的优秀范例。
