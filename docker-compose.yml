services:
  factory-sync:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: factory-sync
    restart: unless-stopped
    environment:
      PRODUCTION_TYPES: ${PRODUCTION_TYPES:-5}
      TZ: Asia/Shanghai
